import 'package:flutter/material.dart';

class VideosOnWatchScreen extends StatelessWidget {
  const VideosOnWatchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}